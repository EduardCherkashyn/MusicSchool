{% extends 'base.html.twig' %}

{% block title %}Редактировать урок{% endblock %}

{% block body %}
    <div class="container">
        <h1>Редактировать урок:</h1>

        {{ include('lesson/_form.html.twig', {'button_label': 'Редактировать'}) }}

        <a href="{{ path('lesson_index') }}" class="btn btn-primary mb-3">назад к списку</a>

         {{ include('lesson/_delete_form.html.twig') }}
    </div>
{% endblock %}
