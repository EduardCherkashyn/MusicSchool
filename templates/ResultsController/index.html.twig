{# ./templates/default/mypdf.html.twig #}
<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8"/>
    <title>Title of the PDF</title>
</head>
<body>
<style>
    body {
        font-family: DejaVu Sans, serif;
    }
    #table{
        max-width: 2480px;
        width:100%;
        border: 1px solid black;
    }
    #table td{
        width: auto;
        overflow: hidden;
        word-wrap: break-word;
        border: 1px solid black;
    }

    th,tr{
        border: 1px solid black;
    }


</style>
    <h1> Звіт класу Черкашина Олександра Олександровича за {{ year}}&nbsp;рік місяць {{ month }}</h1>

    <table id="table" cellspacing="0">
        <thead>
        <tr>
            <th width="130px">Ім'я</th>
            <th width="100px">Дата</th>
            <th width="500px">Завдання</th>
            <th width="200px">Коментар</th>
            <th width="10px">Оцінка</th>
        </tr>
        </thead>
        <tbody>
    {% for lesson in data %}
        <tr>
            <td>{{ lesson.student.name }}</td>
            <td>{{ lesson.date|date("d/m/Y") }}</td>
            <td>{{ lesson.homework }}</td>
            <td>{{ lesson.markComment}}</td>
            <td>{{ lesson.mark}}</td>
        </tr>
        <tr>
            <td colspan="5">Відео звіт:</td>
        </tr>
        {% for link in lesson.youtubeLinks %}
                <tr>
                    <td colspan="5">{{ link.path }}</td>
                </tr>
        {% endfor %}
        <tr>
            <td colspan="5" style="height:100px;"></td>
        </tr>
    {% endfor %}
        </tbody>
    </table>
</body>
</html>
